// Version marker consumed by TSFooter (import VERSION from "../../VERSION").
// Previously generated at build time under CRA; under Vite we commit it and
// keep it in sync with package.json so the footer shows the real dashboard
// version without a custom build step.
export default '0.12.3';
